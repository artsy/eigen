name: Setup Ruby
description: "Set up Ruby with bundler cache and install dependencies"
runs:
  using: "composite"
  steps:
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: ".ruby-version"
        bundler-cache: true

    - name: Install Ruby dependencies
      run: bundle install
      shell: bash
