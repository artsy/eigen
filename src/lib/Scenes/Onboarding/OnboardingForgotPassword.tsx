import { Flex, Text } from "palette"
import React from "react"

export const OnboardingForgotPassword = () => {
  return (
    <Flex flex={1} justifyContent="center" alignItems="center">
      <Text variant="largeTitle">Forgot password</Text>
    </Flex>
  )
}
