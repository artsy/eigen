import { requireNativeComponent } from "react-native"

// requireNativeComponent automatically resolves 'RNTMap' to 'RNTMapManager'
export const ARTTestNativeView = requireNativeComponent("ARTTestNativeView")
