import { LotsByFollowedArtists } from "../LotsByFollowedArtists"
import { mount } from "react-native"

describe("LotsByFollowedArtists", () => {
  it("should work", () => {
    const container = mount(<LotsByFollowedArtists />)
  })
})
