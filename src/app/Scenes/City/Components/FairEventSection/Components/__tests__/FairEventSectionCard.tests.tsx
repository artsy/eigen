import { FairEventSectionCard } from "app/Scenes/City/Components/FairEventSection/Components/FairEventSectionCard"

describe(FairEventSectionCard, () => {
  it.todo("No tests yet, it's just placeholders so far.")
})
