import { CityGuideFairEventSectionCard } from "app/Scenes/CityGuide/Components/CityGuideFairEventSectionCard"

describe(CityGuideFairEventSectionCard, () => {
  it.todo("No tests yet, it's just placeholders so far.")
})
