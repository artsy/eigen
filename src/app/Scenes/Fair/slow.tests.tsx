import { Flex, Theme } from "@artsy/palette-mobile"

describe("slow flex", () => {
  it("does thing", () => {
    const flex = Flex
    expect(flex).toBe(flex)
  })

  it("does other thing", () => {
    const theme = Theme
    expect(theme).toBe(theme)
  })
})
