module.exports = {
  dependencies: {
    zeego: {
      platforms: {
        // Skip auto-linking here because we don't use it on android
        android: null,
      },
    },
    "@react-native-menu/menu": {
      platforms: {
        // Skip auto-linking here because we don't use it on android
        android: null,
      },
    },
  },
}
