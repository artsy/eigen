source 'https://github.com/artsy/Specs.git'
source 'https://github.com/CocoaPods/Specs.git'

use_frameworks!

target 'Emission_Example' do
  pod 'Emission', :path => '../'
  pod 'React', '0.21.0-rc' #, :subspecs => %w( RCTWebSocket RCTNetwork )
end

target 'Emission_Tests' do
  pod 'Emission', :path => '../'

  pod 'Quick', '~> 0.8.0'
  pod 'Nimble', '3.0.0'
  pod 'FBSnapshotTestCase', :git => 'https://github.com/neonichu/ios-snapshot-test-case.git'
  pod 'Nimble-Snapshots'
end
